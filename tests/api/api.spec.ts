import { test, expect } from '@playwright/test';

test(`Obtain token`, { tag: ['@api'] }, async ({ page }) => {
  // to be implemented
  // https://api.inv.bg/v3/swagger-ui/#operation/getLoginToken
});

// Obtain token
// Create item
// Get item
// Delete item
